# Environment
.env

# Python
.venv/
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
dist/
build/

# Pytest
.pytest_cache/

# Notes inbox — keep the folder structure but not imported notes
notes-inbox/processed/

# Editor / IDE (personal dev config, e.g. debug launch.json)
.vscode/

# Throwaway exploration scripts
scratch.py

# Claude Code tooling (personal dev config, e.g. launch.json for previews)
# — except commands/, which holds shared project slash commands (checked in)
.claude/*
!.claude/commands/

# Local
NOTES.md